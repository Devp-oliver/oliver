using System;

namespace Work {
    class collect {
        public static void Main(string[] args) {
            int l;   
            double m;
            string ass;
           
            int i = 6;
            double f = 4.5;
            string s = "polytechnic ";

            Console.Write("Supply the integer value: ");
            l = int.Parse(Console.ReadLine()); 
            Console.Write("Supply the double value: ");
            m = double.Parse(Console.ReadLine()); 

            Console.Write("Enter the string value: ");
            ass = Console.ReadLine();

            int sumInt = i + l;
            double sumdouble = d + m;

            Console.WriteLine(sumInt);
            Console.WriteLine(sumdouble);
            Console.WriteLine(s + ass);
        }
    }
}
